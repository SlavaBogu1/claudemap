# Indexer ↔ Visualizer API Contract

**Owner:** Indexer team. **Version:** v0.1 (skeleton — no endpoints defined yet).
Golden copy — the Visualizer reads this file directly; never copy it into `visualizer/`.
Change workflow: `REQUIREMENTS/PRODUCT_OWNER_PROCESS.md` § Contract Change Workflow.

## Status
This is a placeholder created at project bootstrap (`REQUIREMENTS/DECISIONS.md` D3). The actual v1
endpoint list and JSON schemas (sessions, graph nodes/edges, decisions, bookmarks) are defined by the
**Indexer** team as part of its first sprint task, then versioned here with a changelog entry per
change (per the Contract Change Workflow).

## Changelog
- **v0.1** (2026-07-02) — skeleton created at bootstrap; no endpoints yet.
