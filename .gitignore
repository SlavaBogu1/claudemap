# Dependencies
node_modules/

# Build output
dist/
build/

# Indexer local data: index.db (rebuildable cache) + annotations.db (durable user
# bookmarks/links — irreplaceable, not backed up by git; see D16) (REQUIREMENTS/SHARED_CONSTANTS.md)
indexer/data/

# Env / local config
.env
.env.local

# Logs
*.log
npm-debug.log*

# OS cruft
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# --- SDLC process management (local-only — never pushed; REQUIREMENTS/DECISIONS.md D11) ---
# Stays on disk so the ProductOwner/team agents keep working; only actual code + the API
# contract are project artefacts that belong on GitHub.
/README.md
/PRODUCT_OWNER_PROCESS.md
/CLAUDE.md
/roles/
/skills/
/templates/
/REQUIREMENTS/
/ProductOwner_records/
/.claude/
/indexer/CLAUDE.md
/indexer/TASKS.md
/indexer/requirements/
/visualizer/CLAUDE.md
/visualizer/TASKS.md
/visualizer/requirements/
/tester/
